# AGENTS.md｜鹰眼（EagleEye）Demo 生成与协作约束

> **用途**：给“AI 代理/外包/新同事”看的执行规范。  
> **目标**：在实现 Stage2 纯前端原型 Demo 的过程中，严格遵守 **隐私红线、目录规范、测试要求、输出物要求**，避免跑偏。

---

## 0. 项目目标（必须先读）

### 0.1 Demo 验证点（Stage2）
- ✅ **闯入/徘徊告警**：告警列表、处置流程（确认/关闭）、置信度展示
- ✅ **疑似跌倒告警**：同上（“辅助告警”口径）
- ✅ **无图像监控大屏**：不展示人像画面，仅展示事件与指标
- ✅ **门禁/访客系统对接（演示级）**：前端模拟 Webhook payload 与“核验结果”UI（无后端）

### 0.2 Demo 不做什么（硬性边界）
- ❌ 不展示、不过滤、不回放任何可识别肖像的视频/图片
- ❌ 不存储 DVS 原始事件流 / WiFi 原始数据（Demo 也不允许写入本地文件）
- ❌ 不做人脸/身份识别
- ❌ 不做跨房间轨迹追踪、精确定位（Stage2 仅“房间级”）
- ❌ 不引入后端服务（本仓库 **纯前端模拟**）

---

## 1. 技术栈与目录结构（必须遵守）

### 1.1 技术栈
- React + Vite + TypeScript
- TailwindCSS（统一样式体系）
- Three.js（通过 `@react-three/fiber` / `@react-three/drei`）
- Vitest + Testing Library（组件测试）
- 纯前端 mock（数据来自 `src/mock-data`）

### 1.2 目录规范（固定）
```
src/
  pages/            # 页面（路由级）
  components/       # 可复用组件（必须有测试）
  utils/            # 工具函数/类型定义
  mock-data/        # 纯前端 mock 数据（devices/alarms/...）
```

### 1.3 文件命名约定
- 组件：`PascalCase.tsx`
- 组件测试：`ComponentName.test.tsx`（与组件同目录）
- 页面：`PascalCasePage.tsx`（例如 `AlarmsPage.tsx`）
- mock 数据：`kebab-case.ts` 或 `lowercase.ts`（例如 `alarms.ts`）

---

## 2. UI/视觉约束（必须遵守）

### 2.1 风格关键词
- 暗黑风格
- 卡片式布局
- “装备/控制台”气质（工业感、克制、可读性强）

### 2.2 Three.js（Web3D）要求
- 必须有一个“固定房间”3D 视图（示意即可）
- 表现“房间四角监控点位”
- 当有未处理告警时，3D 视图需要有明显的视觉反馈（颜色/光晕/状态）

---

## 3. 测试约束（必须遵守）

### 3.1 组件测试硬要求
- `src/components/` 下 **每一个组件文件**必须有对应的 `.test.tsx`
- 测试至少覆盖：
  - 组件能渲染
  - 关键文案/关键交互（按钮点击、弹窗打开等）

### 3.2 命令
```bash
npm run dev
npm run test
npm run test:watch
```

---

## 4. Mock 数据与隐私约束

### 4.1 数据来源
- 所有数据必须来自 `src/mock-data/`
- 页面层允许 `useState(seed)` 做“本地交互态”，但不得写入文件或持久化存储

### 4.2 隐私输出约束（强制）
- UI 只能展示：事件类型、区域、时间、置信度、处置状态、设备健康等
- 不允许出现“图像/帧/回放”相关的功能入口

---

## 5. 交付物清单（Demo 达标条件）

交付前必须检查：
- [ ] 页面可运行（`npm run dev`）
- [ ] 组件测试全绿（`npm run test`）
- [ ] 目录结构符合规范（pages/components/utils/mock-data）
- [ ] Three.js 场景存在且与告警状态联动
- [ ] 无后端依赖、无云依赖、无原始流存储

---

## 6. 变更规范（防止 Demo 失控）

任何新增需求都要先回答：
1) 是否属于 Stage2（闯入/徘徊、疑似跌倒、无图像大屏、门禁对接演示）？  
2) 是否触碰隐私边界（产生可识别画面/存储原始流/上云）？  
3) 是否会引入后端（禁止）？  

不满足则拒绝实现或推迟到 Stage3。

