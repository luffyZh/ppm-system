# PRD｜隐私防护监控装备：鹰眼（EagleEye）

> **文档目的**：明确产品定义、功能边界与 Stage2（核心需求验证）MVP 范围，形成可对齐研发/交付/客户的统一口径。  
> **核心原则**：**不采集、不展示、不存储可识别肖像的视频/图像**；系统默认全本地闭环，仅输出“事件与指标”。  

---

## 1. 背景与机会

在涉密核心区域与老人看护（尤其浴室）等场景中，传统 RGB 监控常因以下原因不可用：
- 合规/涉密：禁止摄像头或禁止产生可还原人像的内容。
- 心理接受度：即便“打码/脱敏”，仍被认为有泄露风险。
- 场景稳定性：浴室潮湿、反光、遮挡、逆光/弱光等导致传统视觉方案误报/漏报治理成本高。

**机会点**：以“非传统感知 + 多模态冗余 + 本地推理与审计”交付可落地的安全监测能力，解决“不能拍但必须管”的刚需。

---

## 2. 产品定义（我们是谁）

### 2.1 产品一句话
**鹰眼是一套面向“摄像头禁区”的本地化隐私监控装备：不输出画面，只输出风险事件（闯入/徘徊、疑似跌倒），并可联动门禁/访客系统。**

### 2.2 目标场景（Stage2 聚焦）
1) **涉密核心区域/高敏感区域（ToB）**  
数据中心/机房、涉密办公室、核心仓库、实验室、产线关键工位、金融/政务敏感区域等。

2) **老人看护关键区域（ToB）**  
养老院/护理机构的浴室、床边、夜间走廊等“高风险且强隐私”区域。

### 2.3 目标客户与购买方
- 购买方：甲方安保负责人、信息安全负责人、设施管理/后勤负责人；养老机构运营负责人/护理负责人。
- 使用方：安保值班人员/护士站、巡检人员、门禁管理员。

---

## 3. 用户画像（Personas）

### P1 安保/涉密区域管理员（核心）
- 目标：核心区域“有人闯入/异常逗留”必须可发现、可追溯、可审计。
- 痛点：摄像头禁用或难以通过审查；误报多会导致“告警疲劳”；事后追责缺证据链。

### P2 护理人员/护士站（核心）
- 目标：浴室/床边跌倒能及时发现，减少“长时间倒地”风险。
- 痛点：浴室不能装摄像头；穿戴设备依从性差；夜间人手不足。

### P3 合规/信息安全负责人（强影响）
- 目标：数据不出域、无可识别图像、权限审计完备。
- 痛点：任何“可还原画面”的方案都可能被一票否决。

---

## 4. 核心痛点与价值（要解决什么）

### 核心痛点（按优先级）
1. **摄像头不可用**：无法部署 RGB 监控仍需满足安全/看护管理要求。  
2. **漏报成本极高**：闯入与跌倒类事件一旦漏报，安全/责任后果严重。  
3. **告警可运营**：误报不可过高，否则“被关掉”。  
4. **可审计可交付**：需要本地化、权限、审计日志、可对接现有系统。

### 价值主张
- 用“无图像化监测”覆盖摄像头禁区；
- 用多模态冗余设计降低漏报；
- 用本地化与审计能力满足涉密/隐私要求；
- 通过对接门禁/访客系统形成闭环处置。

---

## 5. 产品范围与功能边界（最重要）

### 5.1 In Scope（Stage2 MVP 必须交付）
> 你已确认：**闯入/徘徊告警 + 疑似跌倒告警** 为 MVP 必选。

**A. 闯入/徘徊告警（涉密核心区）**
- 检测：进入事件、停留超时（徘徊/逗留）、非工作时间进入等规则触发。
- 输出：事件类型、时间、持续时长、置信度、处置建议（联动门禁/访客核验）。

**B. 疑似跌倒告警（浴室/老人看护）**
- 检测：疑似跌倒事件（“疑似 + 置信度 + 人工确认”口径）。
- 输出：事件类型、时间、持续时长、置信度、建议处置（电话/广播/巡检）。

**C. 本地监控界面（无图像大屏）**
- 房间/点位状态：在线、最近心跳、当前状态（正常/疑似异常/告警中）。
- 告警列表：时间线、置信度、状态（未处理/处理中/已关闭）、备注。

**D. 门禁/访客系统对接（优先）**
- 输出：事件推送（Webhook/HTTP API/消息队列任选其一，Stage2 可先做 HTTP Webhook）。
- 场景：告警触发后，联动拉取访客记录/授权名单（若客户提供接口）。

### 5.2 Out of Scope（Stage2 明确不做，避免范围失控）
1. **不提供任何可识别肖像的视频/图片/帧**（包括“灰度视频、热成像伪彩、可还原轮廓的回放”等）。  
2. **不做人脸识别/身份识别/人员画像**（涉密风险高、边界复杂）。  
3. **不做跨房间轨迹追踪/精确定位**（Stage2 仅房间级/点位级）。  
4. **不做医疗级诊断**（跌倒为“辅助告警”，不承担医疗诊断责任）。  
5. **不依赖云端**（默认全本地；云管理仅作为未来可选项，不在 Stage2）。  

---

## 6. 隐私与合规承诺（对外口径）

你已确认采用最强承诺：
- **全本地部署**：采集、推理、存储、展示、对接均在本地网络内完成。  
- **不存储原始流**：不落盘 DVS 原始事件流与 WiFi 原始数据；仅保存“事件结果与统计指标”。  
- **最小化输出**：对外输出仅为事件、置信度、时间戳、持续时长、设备状态等。  
- **审计能力**（Stage2 最小集）：登录与操作审计、告警处理留痕、配置变更记录。  

> 说明：若未来客户强烈要求“取证”，应走“可选短环形缓存 + 严格权限与审计”的高级版能力（不在 Stage2）。

---

## 7. 关键用户旅程（User Journeys）

### 7.1 涉密核心区：闯入/徘徊
1) 系统检测到“进入/逗留超时” → 生成告警（置信度）。  
2) 大屏弹出告警 + 声光提示（可配置）。  
3) 值班人员点击告警 → 查看详情（时间、持续、置信度、处置建议）。  
4) 系统调用门禁/访客接口核验（如：是否有授权/是否访客登记）。  
5) 人员处置后关闭告警，填写备注 → 形成审计记录。

### 7.2 浴室：疑似跌倒
1) 系统检测疑似跌倒 → 告警。  
2) 护理人员确认（电话/对讲/敲门/现场查看） → 选择“真实/误报/无法确认”。  
3) 系统记录确认结果，用于后续阈值/模型迭代（仅保存标签，不保存原始流）。

---

## 8. 功能需求（Functional Requirements）

### 8.1 告警引擎（通用）
- 告警状态机：未处理 / 已确认 / 已关闭 / 升级处置（可选）。  
- 去抖/冷却：避免同一事件短时间重复告警。  
- 置信度：0~1 或 0~100 的统一刻度，前端以颜色/等级展示。  
- 规则配置（Stage2 最小集）：  
  - 进入即告警（开/关）  
  - 逗留超时阈值（如 60s/120s/300s）  
  - 时段策略（工作时间/非工作时间）  

### 8.2 大屏与后台
- 实时状态总览：点位卡片、在线状态、最近心跳时间。  
- 告警列表：筛选（点位/类型/状态/时间）、详情页、处理记录。  
- 用户权限（最小 RBAC）：管理员/值班员/只读审计。  

### 8.3 对接能力（门禁/访客）
- 告警推送：Webhook（HTTP POST）  
- 访客核验：客户提供 API 时支持查询（Stage2 可仅做“预留接口 + mock”以便试点演示）  
- 失败降级：对接失败不影响本地告警闭环，记录错误日志。

---

## 9. 非功能需求（NFR）与验收指标（Stage2 建议）

### 9.1 可用性与性能
- 系统可用性：7×24 本地运行；断电恢复后自动启动。  
- 告警延迟：从事件发生到告警展示 **≤ 2s（目标）**。  

### 9.2 准确性（以“低漏报”为主）
> Stage2 的重点是“证明可用”，建议先用“可量化的现场试验脚本”验收。

- 闯入检测漏报：在指定试验脚本下 **漏报率 ≤ X%（待定）**  
- 跌倒疑似漏报：在指定试验脚本下 **漏报率 ≤ X%（待定）**  
- 误报约束：**每天误报不超过 Y 次/点位（待定）**（否则会被弃用）  

（注：X、Y 需要你在试点前与客户对齐；PRD 暂保留为“待定”，但应要求在试点启动前冻结。）

### 9.3 隐私与安全
- 默认不落盘原始流；仅保存事件与指标。  
- 审计日志不可被普通用户删除。  
- 本地网络访问控制（白名单/固定端口/账号策略）。

---

## 10. 方案概述（供研发与交付对齐）

### 10.1 多模态输入（建议）
- 主：DVS 事件相机（动目标、瞬态事件）  
- 辅：WiFi Sensing（RuView 思路/组件可复用，用于遮挡与静止兜底）  

### 10.2 处理与输出（Edge Box）
- 输入接入层：事件流/特征流接入、时间戳对齐（滑窗融合）。  
- 推理层：闯入/徘徊、疑似跌倒检测；置信度融合与规则引擎。  
- 输出层：本地大屏 + Webhook 对接门禁/访客。  
- 数据层：仅事件与指标存储（本地 DB），审计日志。

---

## 11. 里程碑（Stage2）

> 目标：2–4 周内完成单点位/单房间可演示、可试点验证的 MVP。

1) **W1：场景冻结 + 最小链路跑通**（设备上线、数据接入、大屏出状态）  
2) **W2：两类告警 MVP**（闯入/徘徊 + 疑似跌倒，含置信度与去抖）  
3) **W3：对接与审计最小集**（Webhook、基本权限、操作留痕）  
4) **W4：现场脚本验收 + 试点复盘**（指标冻结，进入 Stage3 扩点）  

---

## 12. 风险与对策（提前说清楚）

1) **DVS 对静止目标不敏感** → 用 WiFi Sensing 做“存在/长静止”兜底，减少漏报。  
2) **浴室复杂环境（遮挡/反光/湿气）** → 试点必须覆盖“最差环境”脚本；必要时引入第三模态（mmWave/PIR）作为可选增强。  
3) **误报导致告警疲劳** → 强制引入去抖/冷却与“辅助告警”口径（置信度 + 人工确认）。  
4) **客户对接不确定** → Stage2 先实现 Webhook 输出与 mock；现场对接做“渐进式”交付。  

---

## 13. 未决问题（Open Questions，需要你补充/决策）

1) Stage2 试点的**点位数量与空间**：涉密核心区 1 点位？还是同时做浴室 1 点位？  
2) DVS 硬件选型与预算区间：偏“开发套件验证”还是“可量产模块”？  
3) 告警处置链路：仅大屏提示？还是需要短信/电话/对讲联动（后续可扩展）？  
4) 客户门禁/访客系统类型与接口形态：是否能提供 API 文档/测试环境？  

